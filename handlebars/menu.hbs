<ul id="items">
	{{#each item}}
  <li data-id="{{id}}">
    <article>
      <header>
        <figure>
          <img src="{{imagePath name}}" alt="menu-item">
        </figure>
        <h2 class="name">{{name}}</h2>
      </header>
      <p class="price">{{formatPrice price}}</p>
      <footer>
        <a href="#" class="add_cart">Add to cart</a>
      </footer>
    </article>
  </li>
  {{/each}}
</ul>
